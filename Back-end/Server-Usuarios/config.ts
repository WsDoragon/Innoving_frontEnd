export const config = {
    db: {
        user: "root",
        host: "localhost",
        password: "1234",
        database:"nodedb",
        waitForConnections:true,
        connectionLimit: 30,
        queueLimit:10
    }
  };