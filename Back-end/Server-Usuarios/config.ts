export const config = {
    db: {
        user: "root",
        host: "localhost",
        password: "_",
        database:"nodedb",
        waitForConnections:true,
        connectionLimit: 30,
        queueLimit:10
    }
  };