export const config = {
    db: {
        user: "root",
        host: "localhost",
        password: "2507",
        database:"nodedb",
        waitForConnections:true,
        connectionLimit: 30,
        queueLimit:10
    }
  };